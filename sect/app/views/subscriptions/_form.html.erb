<%= form_with(model: subscription, local: true) do |form| %>
  <% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
      <% subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, class: "form-control col-6" %>
  </div>

  <div class="field">
    <%= form.label :matricula %>
    <%= form.number_field :matricula, class: "form-control col-6" %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= collection_select(:subscription, :course_id, @cursos, :id, :titulo) %>
  </div>

  <div class="actions">
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
